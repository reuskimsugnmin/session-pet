> 이 문서의 원본은 개발 저장소 `user-docs/install.md` 입니다 — 여기서 직접 고치지 마세요. 릴리스마다 덮어씁니다.

# 설치 안내

세션 펫을 처음 설치하고, 펫이 여러분의 AI 코딩 세션을 보여줄 때까지의 안내입니다. 기술 배경이 없어도
따라올 수 있게 썼습니다. 이 문서는 **배포용 앱(DMG)** 기준입니다 — 저장소를 내려받아 `npm run dev` 로
개발 실행하는 방법은 `app/README.md` 를 보세요.

막히면 `user-docs/셋업과-문제해결.md` 로 이동하세요. 그 문서는 세션이 "안 보일 때"를 다룹니다.

---

## 1. 필요한 것

| 항목                                     | 필수 여부 | 비고                                                                                                                                                                |
| ---------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| macOS, Apple Silicon (M1 이상)           | 필수      | 현재 배포는 **arm64 전용**입니다. Intel Mac 용 빌드는 아직 없습니다(미확인)                                                                                         |
| Claude Code (`claude` CLI)와 Claude 구독 | 필수      | 세션 펫은 여러분이 이미 실행 중인 Claude Code(또는 Codex) 세션을 찾아서 보여줍니다. 펫 자체가 에이전트를 실행하지는 않습니다                                        |
| Orca                                     | 선택      | 없어도 세션 인식·표시·요약·메모는 됩니다. **있으면** 추가로 되는 것: 입력창에 다음 지시를 자동으로 채워 넣기(Orca 안 패널), "지금 보고 있는 세션" 을 더 정확히 판별 |
| Codex CLI / Codex 앱                     | 선택      | Codex CLI, Codex 앱(Desktop) 세션 모두 인식됩니다. 단 서브에이전트·리뷰용 내부 스레드는 자동으로 걸러집니다                                                         |

**"필수"인 이유** — 세션 펫은 여러분 컴퓨터에서 이미 돌고 있는 에이전트 세션의 기록 파일(Claude Code
`~/.claude/sessions/`, Codex `~/.codex/sessions/`)을 읽어서 세션을 찾습니다. 그 기록이 없으면 보여줄
것이 없습니다.

### 비용에 대해 숨기지 않고 말씀드립니다

세션 펫은 각 세션의 "지금 상태"를 요약하기 위해 내부적으로 `claude -p` (Claude Haiku)를 호출합니다.
**보통은 여러분의 Claude 구독 사용량을 씁니다.** 하지만 계정 상태에 따라 — 예를 들어 구독 한도를
넘겼거나 계정이 API 조직에 연결돼 있으면 — **API 크레딧으로 청구될 수 있습니다** (2026-09-08 실측
사례, `docs/dev-notes.md`). 이 호출은 턴이 끝날 때만, 시간당 상한 안에서만 일어나고 얼마나 호출됐는지는
"진단 정보 복사"에서 확인할 수 있습니다(§6).

---

## 2. 앱을 열기

1. DMG 파일을 받아 엽니다.
2. 앱을 `Applications` 폴더로 끌어다 놓습니다.
3. 앱을 실행합니다.

**배포되는 앱은 Developer ID 로 서명하고 Apple 공증(노타라이즈)을 받습니다** (2026-09-18 부터 — `app/electron-builder.config.cjs`).
공증된 앱은 Gatekeeper 경고 없이 열려야 하지만, **다른 기계에서 실제로 열어 확인하기 전까지는 미확인**입니다
(`docs/manual-checklist.md` K11).

공증된 앱은 아래 경고가 뜨지 않아야 합니다. **그래도 뜬다면**(공증 전 빌드를 받았거나 공증이 실패한 경우) 아래를 따르세요:

- **macOS 15(Sequoia) 이상으로 알려져 있습니다**: 앱을 처음 열면 macOS 가 열기를 막고 "확인할 수
  없는 개발자입니다" 류의 경고만 보여줍니다. **Finder 우클릭 → 열기로는 더 이상 우회되지 않습니다** —
  한 번 실행을 시도해 막힌 뒤, **시스템 설정 → 개인정보 보호 및 보안**으로 가서 "그래도 열기"를 눌러야
  한다고 알려져 있습니다.
- **macOS 14 이하로 알려져 있습니다**: Finder 에서 앱을 **우클릭 → 열기**로 한 번 더 승인하면 열립니다.

이 절은 이 앱으로 실측되는 대로 갱신합니다.

앱은 Dock 아이콘 없이 뜹니다(`LSUIElement`). 화면 한쪽에 펫이 나타납니다.

---

## 3. 권한

세션 펫이 실제로 요구하는 macOS 권한은 다음과 같습니다.

- **자동화(Automation) 권한** — AppleScript(`osascript`)로 iTerm2·cmux 를 제어할 때 씁니다. 아래 두
  경우 모두에 쓰이고, macOS 가 "세션 펫이 iTerm2를 제어하려고 합니다" 같은 승인 창을 띄우면 허용해야
  두 기능이 다 동작합니다. 거부해도 앱은 죽지 않습니다 — 주입은 클립보드 복사로 대체되고, 판별은
  다른 방법(직전 세션 유지)으로 넘어갑니다.
  - 입력창에 텍스트를 채워 넣을 때(주입)
  - iTerm2·Terminal 에서 **같은 앱에 세션이 여러 개 열려 있어 "지금 보고 있는 탭"을 구분해야 할 때**(판별) —
    이건 여러분이 아직 아무 지시도 채워 넣지 않았어도, 펫이 세션을 추적하는 동안 자동으로 일어날 수
    있습니다. 그래서 **자동화 승인 창은 주입을 한 번도 하지 않아도 뜰 수 있습니다.** 판별용 자동화
    호출이 필요 없으면 `~/.session-pet/config.json` 에 `{ "focus": false }` 를 넣거나(개발 실행 시
    `PET_NO_FOCUS=1`) 꺼서 아예 시도하지 않게 할 수 있습니다 — 대신 "지금 보고 있는 세션" 추적
    정확도가 낮아집니다(최근 상태가 바뀐 세션으로 대체).
- **접근성(Accessibility) 권한은 요구하지 않습니다.** 세션 펫은 macOS 접근성 API(`System Events`
  의 `keystroke` 등)를 쓰지 않습니다 — 이 방식은 2026-09-08 오너 결정으로 범위 밖입니다
  (`docs/dev-notes.md`). ROADMAP 의 예전 문구("접근성 권한(2차)")는 이 결정 이전의 계획이며 지금
  코드와 맞지 않습니다.
- 그 밖의 macOS 권한 요청(마이크·카메라·연락처 등)은 없습니다.

**펫은 여러분의 에이전트 기록과 설정을 읽기만 합니다.** `~/.claude`, `~/.codex` 의 세션 기록은 읽기
전용이며, 사용자의 Claude Code 설정(`~/.claude/settings.json`)을 고치는 코드는 없습니다. 펫은 입력창을
채우기만 하고, Enter 는 누르지 않습니다. 전송은 항상 사람이 합니다.

---

## 4. Dock/Launchpad 에서 실행할 때 — 명령어를 못 찾는 경우

Dock 이나 Launchpad 에서 띄운 앱은 macOS 가 아주 좁은 PATH(`/usr/bin:/bin:/usr/sbin:/sbin`)만 줍니다.
`orca`·`claude`·`tmux` 같은 실행 파일은 보통 이 경로 밖(`/usr/local/bin`, `/opt/homebrew/bin`,
`~/.local/bin` 등)에 있습니다.

앱은 켜질 때 한 번 로그인 셸을 실행해 PATH 를 스스로 알아냅니다. 셸 설정이 일반적이지 않아 이 방법이
실패하면, 우클릭 → **"진단 정보 복사"**로 "PATH 보강" 줄을 확인하세요. `셸 프로브 실패`면:

1. 터미널에서 `which orca claude` 로 실제 경로를 확인합니다.
2. `~/.session-pet/config.json` 파일을 만들거나 열어 경로를 직접 지정합니다.

   ```json
   {
     "version": 1,
     "bins": {
       "orca": "/usr/local/bin/orca",
       "claude": "/Users/나/.local/bin/claude"
     }
   }
   ```

3. 앱을 다시 켭니다.

자세한 원인과 다른 설정 키는 `app/README.md` "설정 파일" 절, 막힌 상황별 대처는
`user-docs/셋업과-문제해결.md`를 보세요.

---

## 5. 데이터: 무엇을 읽고 어디에 저장하는가

- **읽기 전용으로 읽는 것**: Claude Code 세션 기록(`~/.claude/sessions/`, `~/.claude/projects/`),
  Codex 세션 기록(`~/.codex/sessions/`), Orca 가 있으면 그 CLI 출력과
  `~/Library/Application Support/orca/agent-hooks/last-status.json`.
- **쓰는 곳은 `~/.session-pet/` 뿐입니다** — 세션별 요약·대기열, 창 위치 같은 상태, 설정 파일
  (`config.json`), 로그(`logs/pet.log`, 5MB 씩 2개 순환).
- **로그와 "진단 정보 복사"에는 대화 내용이 담기지 않습니다.** 프롬프트·응답·요약·메모는 글자 수로만
  기록됩니다. 그대로 이슈나 메시지에 붙여도 안전합니다.
- **자동 종료**: 추적할 세션이 0개인 상태가 이어지면(기본 60초) 펫이 스스로 꺼집니다. 계속 띄워
  두고 싶으면 `~/.session-pet/config.json` 에 `{ "version": 1, "autoQuitMs": 0 }` 를 넣으세요.

---

## 6. 문제가 생기면

1. 펫을 **우클릭** → **"진단 정보 복사"**
2. 아래 주소로 그대로 붙여넣어(⌘V) 보내 주세요 — **<smkim931206@gmail.com>** (알파 피드백 창구, 오너 결정 2026-09-18)

앱·시스템 버전, 세션 개수와 상태, 환경 점검 결과, 오류 메시지가 담기고 대화 본문은 담기지 않습니다.
더 자세한 기록은 `~/.session-pet/logs/pet.log` 에 있습니다.

세션이 있는데 펫이 잠들어 있거나, Orca 를 못 찾거나, 요약이 안 나오는 등 구체적인 증상별 원인과
조치는 `user-docs/셋업과-문제해결.md`를 확인하세요. (그 문서는 초기 PoC 실행 기준으로 쓰인 절이
섞여 있어 일부 표현이 배포 앱과 다를 수 있습니다 — 다르면 이 문서가 우선입니다.)
